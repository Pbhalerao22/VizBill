<div class="modal-overlay">
    <div class="modal">

        <div class="modal-header">
            <h2>Add Item</h2>
            <span class="close-btn">&times;</span>
        </div>

        <div class="form-group">
            <label>Item Name</label>
            <input type="text" placeholder="e.g. Masala Chai">
        </div>

        <div class="form-group">
            <label>Price (₹)</label>
            <input type="number" placeholder="e.g. 20">
        </div>

        <div class="form-group">
            <label>Category</label>
            <div class="category-chips">
                <button type="button" class="chip">Drinks</button>
                <button type="button" class="chip active">Snacks</button>
                <button type="button" class="chip">Fast Food</button>
            </div>
        </div>

        <button class="submit-btn">Add Item</button>

    </div>
</div>

<style>
    /* Overlay */
    .modal-overlay {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.2);
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: 'Segoe UI', sans-serif;
    }

    /* Modal Box */
    .modal {
        width: 600px;
        background: #f5f5f5;
        border-radius: 18px;
        padding: 25px 30px 30px 30px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    }

    /* Header */
    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 25px;
    }

        .modal-header h2 {
            margin: 0;
            font-size: 22px;
            font-weight: 600;
        }

    .close-btn {
        font-size: 22px;
        cursor: pointer;
        color: #777;
    }

    /* Form */
    .form-group {
        margin-bottom: 20px;
    }

        .form-group label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #333;
        }

        .form-group input {
            width: 100%;
            padding: 14px;
            border-radius: 10px;
            border: 1px solid #ddd;
            background: #fff;
            font-size: 14px;
            outline: none;
            transition: 0.2s;
        }

            .form-group input:focus {
                border-color: #ff7a18;
                box-shadow: 0 0 0 2px rgba(255, 122, 24, 0.15);
            }

    /* Category Chips */
    .category-chips {
        display: flex;
        gap: 12px;
        margin-top: 10px;
    }

    .chip {
        padding: 10px 18px;
        border-radius: 25px;
        border: none;
        background: #e5e5e5;
        cursor: pointer;
        font-weight: 500;
        transition: 0.2s;
    }

        .chip:hover {
            background: #d8d8d8;
        }

        .chip.active {
            background: #ff7a18;
            color: #fff;
        }

    /* Submit Button */
    .submit-btn {
        width: 100%;
        padding: 16px;
        border: none;
        border-radius: 12px;
        background: #ff7a18;
        color: #fff;
        font-size: 16px;
        font-weight: 600;
        margin-top: 15px;
        cursor: pointer;
        transition: 0.2s;
    }

        .submit-btn:hover {
            background: #e66a0f;
        }
</style>